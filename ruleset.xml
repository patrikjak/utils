<?xml version="1.0"?>
<ruleset xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         name="PSR-12 + PER-3 Standard"
         xsi:noNamespaceSchemaLocation="vendor/squizlabs/php_codesniffer/phpcs.xsd">

    <description>PSR-12 and PER-3 coding standards with minimal Slevomat additions for PHP 8.4+</description>

    <!-- Configuration -->
    <config name="show_progress" value="1"/>
    <config name="tab_width" value="4"/>
    <config name="php_version" value="8.5"/>
    <config name="installed_paths" value="vendor/slevomat/coding-standard"/>

    <!-- Command line arguments -->
    <arg name="colors"/>
    <arg name="parallel" value="8"/>
    <arg value="sp"/>
    <arg name="extensions" value="php"/>

    <!-- Files to check -->
    <file>src</file>
    <file>tests</file>

    <!-- ===================================================================== -->
    <!-- PSR-12 BASE STANDARD                                                  -->
    <!-- ===================================================================== -->

    <!-- Include full PSR-12 standard -->
    <rule ref="PSR12"/>

    <!-- ===================================================================== -->
    <!-- PER-3 ESSENTIAL ADDITIONS                                             -->
    <!-- ===================================================================== -->

    <!-- Short form type keywords (bool not boolean, int not integer) -->
    <rule ref="SlevomatCodingStandard.TypeHints.LongTypeHints"/>

    <!-- Trailing commas in multiline arrays -->
    <rule ref="SlevomatCodingStandard.Arrays.TrailingArrayComma"/>

    <!-- DNF type formatting -->
    <rule ref="SlevomatCodingStandard.TypeHints.DNFTypeHintFormat"/>

    <!-- Enum case spacing -->
    <rule ref="SlevomatCodingStandard.Classes.EnumCaseSpacing"/>

    <!-- Backed enum type spacing -->
    <rule ref="SlevomatCodingStandard.Classes.BackedEnumTypeSpacing"/>

    <!-- ===================================================================== -->
    <!-- RECOMMENDED ADDITIONS (Best Practices)                                -->
    <!-- ===================================================================== -->

    <!-- Declare strict types -->
    <rule ref="SlevomatCodingStandard.TypeHints.DeclareStrictTypes">
        <properties>
            <property name="spacesCountAroundEqualsSign" value="0"/>
            <property name="declareOnFirstLine" value="false"/>
        </properties>
    </rule>

    <!-- Modern class name reference (::class) -->
    <rule ref="SlevomatCodingStandard.Classes.ModernClassNameReference"/>

    <!-- Alphabetically sorted use statements -->
    <rule ref="SlevomatCodingStandard.Namespaces.AlphabeticallySortedUses"/>

    <!-- Use statement spacing -->
    <rule ref="SlevomatCodingStandard.Namespaces.UseSpacing">
        <properties>
            <property name="linesCountBeforeFirstUse" value="1"/>
            <property name="linesCountBetweenUseTypes" value="1"/>
            <property name="linesCountAfterLastUse" value="1"/>
        </properties>
    </rule>

    <!-- Type hint spacing -->
    <rule ref="SlevomatCodingStandard.TypeHints.ParameterTypeHintSpacing"/>
    <rule ref="SlevomatCodingStandard.TypeHints.ReturnTypeHintSpacing"/>

    <!-- Nullable type format -->
    <rule ref="SlevomatCodingStandard.TypeHints.NullableTypeForNullDefaultValue"/>

    <!-- Class constant visibility -->
    <rule ref="SlevomatCodingStandard.Classes.ClassConstantVisibility"/>

    <!-- Remove useless type hints -->
    <rule ref="SlevomatCodingStandard.TypeHints.UselessConstantTypeHint"/>

    <!-- Useless parentheses -->
    <rule ref="SlevomatCodingStandard.PHP.UselessParentheses"/>

    <!-- Duplicate spaces -->
    <rule ref="SlevomatCodingStandard.Whitespaces.DuplicateSpaces"/>

    <rule ref="SlevomatCodingStandard.TypeHints.ParameterTypeHint"/>
    <rule ref="SlevomatCodingStandard.TypeHints.ReturnTypeHint"/>
    <rule ref="SlevomatCodingStandard.TypeHints.PropertyTypeHint"/>
    <rule ref="SlevomatCodingStandard.Operators.DisallowEqualOperators"/>
    <rule ref="SlevomatCodingStandard.Functions.DisallowEmptyFunction"/>
    <rule ref="SlevomatCodingStandard.Classes.RequireConstructorPropertyPromotion"/>

</ruleset>
